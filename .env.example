# Copy this file to .env.local and fill in values for local development.
# Anything prefixed with NEXT_PUBLIC_ is exposed to the browser, everything else is server-only.

# NEXT_PUBLIC_APP_URL=http://localhost:3000
